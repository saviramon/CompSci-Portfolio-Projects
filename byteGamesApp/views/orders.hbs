{{!-- Citation for node starter project
Adapted from CS340 node-starter-app
URL: https://github.com/osu-cs340-ecampus/nodejs-starter-app
Date Retrieved: Feb 26, 2025 --}}

<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <title>BYTE GAMES | Orders</title>
        <meta name="viewport" content="width=device-width, initial-scale=1" />
      </head>
    <body>
        <header>
            <h1>BYTE GAMES</h1>
        </header>
        <nav>
            <a href="/">Home</a>
            <a href="/games">Games</a>
            <a href="/developers">Developers</a>
            <a href="/customers">Customers</a> 
            <a href="/orders">Orders</a>
            <a href="/ordersgames">OrdersGames</a>
        </nav>
        <main>
            <h2>Browse Orders</h2>
            <section>
            <h3>Refresh in order to see updates or deletions</h3>
                <article>
           {{!-- Create a table --}}
                    <table id="orders-table"> 

                        {{!-- Header section --}}
                        <thead>

                            {{!-- For just the first row, we print each key of the row object as a header cell so we
                            know what each column means when the page renders --}}
                            <tr>
                                {{#each orders.[0]}}
                                <th>
                                    {{@key}}
                                </th>
                                {{/each}}
                                <th>Delete</th>
                            </tr>
                        </thead>
                        {{!-- Body section --}}
                        <tbody>

                            {{#each orders}}
                            <tr>
                                <td>{{this.orderID}}</td>
                                <td>{{this.orderDate}}</td>
                                <td>{{this.customerID}}</td>
                                <td>{{this.gameID}}</td>
                                <td>{{this.quantity}}</td>
                                <td><button onclick="deleteOrder({{this.orderID}})">Delete</button></td>
                            </tr>
                            {{/each}}
                        </tbody>
                    </table>
                    <script src="./js/delete_order.js"></script>
                </article>
                <article>
                    <h2>Add an Order</h2>
                    <form id="add-order-form-ajax">
                        <fieldset>
                            <label for="input-orderdate">Order Date: </label>
                            <input type="date" name="input-orderdate" id="input-orderdate" required/>
                            <label for="input-customerid">Customer ID: </label>
                            <select name="input-customerid" id="input-customerid" required>
                                <option>Select Customer ID</option>
                                {{#each customers}}
                        <option value="{{this.customerID}}">({{this.customerID}}, {{this.firstName}} {{this.lastName}})</option>
                                {{/each}}
                            </select>
                            <label for="input-gameid">Game ID: </label>
                            <select name="input-gameid" id="input-gameid" required>
                                <option>Select Game ID</option>
                                {{#each games}}
                                <option value="{{this.gameID}}">({{this.gameID}}, {{this.title}})</option>
                                {{/each}}
                            </select>
                            <label for="input-quantity">Quantity: </label>
                            <input type="text" name="input-quantity" id="input-quantity" placeholder="0" required/>
                        </fieldset>
                        <input type="submit" value="Add Order">
                    </form>
                        <script src="./js/add_order.js"></script>
                </article>
                <article>
                <h2>Update an Order</h2>
                <form id="update-order-form-ajax">
                    <fieldset>                    
                    <label for="update-orderID">Order ID: </label>
                     <select name="update-orderID" id="orderSelect" required>
                        <option>Select Order ID</option>
                        {{#each orders}}
                        <option value="{{this.orderID}}">{{this.orderID}}</option>
                        {{/each}}
                    </select>
                    <label for="update-orderDate">Order Date: </label>
                    <input type="date" name="update-orderDate" id="update-orderDate" required>
                    

                    <label for="update_customerID"> Customer ID: </label>
                    <select name="update-customerID" id="customerSelect" required>
                        <option>Select Customer ID</option>
                        {{#each customers}}
                        <option value="{{this.customerID}}">({{this.customerID}}, {{this.firstName}} {{this.lastName}})</option>
                        {{/each}}
                    </select>
                    <label for="update_gameID"> Game ID: </label>
                    <select name="update-gameID" id="gameSelect" required>
                        <option>Select Game ID</option>
                        {{#each games}}
                        <option value="{{this.gameID}}">({{this.gameID}}, {{this.title}})</option>
                        {{/each}}
                    </select>
                    <label for="update-orderQuantity">Quantity: </label>
                    <input type="text" name="update-orderQuantity" id="update-orderQuantity" placeholder="0" required></fieldset>

                    <input type="submit" value="Update Order">
                </form>
                <script src="./js/update_order.js"></script>
                </article>
            </section>
        </main>
        <footer>
            <p>&copy; 2025 X'avier Tejada | Christian Duval</p>
        </footer>
    </body>
</html>
